{
  "name": "marboys-billiard-pos",
  "version": "1.0.0",
  "description": "Billiard POS System — Firebase Client-Only App",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "preview": "cd client && npm run preview",
    "install-all": "cd client && npm install"
  },
  "dependencies": {},
  "devDependencies": {}
}
